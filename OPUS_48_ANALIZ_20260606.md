# MINA v2 — TAM SİSTEM DENETİMİ (Opus 4.8)

**Tarih:** 2026-06-06  
**Denetlenen çekirdek:** `mina_position_manager.py`, `main.py`, `mina_entry_orders.py`, `signal_slot_bridge.py`, `merter_dca_manager.py`, `haluk_pdf_parser.py`, `haluk_pdf_visual.py`, `backend/config.py`, `mina_trading_journal.py`, `mina_signal_source.py`, `mina_slot_policy.py`, `scripts/manual_open.py`, `dashboard/dashboard_ws.py`

---

## BÖLÜM 1 — KOD KALİTESİ VE ANAYASAYA UYUM

| Kontrol | Durum | Kanıt |
|---|---|---|
| Sabit USDT yazılmamış | ✅ UYUMLU | Marjin her yerde dinamik: `slot = balance/10`, `margin = slot*0.20` |
| Savunma tetikleyici = SPOT fiyat | ✅ UYUMLU | `check_spot_defense_trigger` doğrudan `current_price` ile |
| DERR'e her işlem yazılıyor | ✅ BÜYÜK ORANDA | `log_position_open`/`log_trade_close` tüm giriş noktalarında |
| Slot hesabı | ✅ UYUMLU | `calculate_slot_size = total_usdt/10` |
| **Merter zaman stopu 48s ↔ 4s** | ⚠️ **ÇELİŞKİ** | Kod 4 saat, anayasa 48 saat diyor |
| `backend/config.py` LeverageConfig | ⚠️ ÖLÜ/HATALI | Eski tablo, motor kullanmıyor |

Marjin dinamik — anayasaya tam uyumlu:

```65:72:backend/config.py
    def calculate_slot_size(self):
        """
        Slot büyüklüğünü hesapla
        Toplam USDT / 10
        """
        total_usdt = self.get_usdt_balance()
        slot_size = total_usdt / self.slot_count
        return slot_size
```

Savunma SPOT fiyat üzerinden (ROE değil) — anayasaya uyumlu:

```586:593:mina_position_manager.py
        if current_stage == 0 and self._is_d1_hit(current_price, entry_price, side):
            return 1
        if current_stage <= 1 and self._is_d2_hit(current_price, entry_price, side):
            return 2
        if current_stage <= 2 and self._is_d3_hit(current_price, entry_price, side) and self._check_d3_sfp(symbol, side):
            return 3
        if self._is_hard_stop_hit(current_price, entry_price, side):
            return 99
```

**⚠️ ÇELİŞKİ 1 — Merter zaman stopu:** Kod 4 saat, anayasa ve docstring'ler 48 saat diyor. İkisi tutmuyor:

```49:49:signal_bot/merter_dca_manager.py
MAX_HOLD_H = 4  # varsayılan; dashboard_settings.merterTimeStopH ile override
```

Anayasa: *"48 saat timeout → breakeven modu"* (`MINA_ANAYASASI.md` satır 162). **Karar gerekiyor:** 4 saat mi 48 saat mi? Şu an dashboard `merterTimeStopH` ayarı ne ise o kazanıyor.

**⚠️ ÇELİŞKİ 2 — Ölü config:** `backend/config.py` içindeki `LeverageConfig.LEVERAGE_RULES` 6x/7x/8x/9x içermiyor ve `defense_1=5, defense_2=10` gibi eski alanlar var. Motor bunu **kullanmıyor** (gerçek kaynak `mina_position_manager.leverage_rules`). Yanıltıcı; silinmeli veya senkronlanmalı.

---

## BÖLÜM 2 — MANUEL İŞLEM MODÜLÜ

| Soru | Cevap |
|---|---|
| Coin/yön/kaldıraç seçilebiliyor mu? | ✅ Evet — `{1,2,3,4,5,10}` |
| Hangi slotu işgal eder? | **Haluk motor slotu** (`source='haluk'`, max 7) |
| Manuel kapatma? | ✅ Dashboard (`close_position`/`close_all`) **ve** terminal |
| Marjin = Slot/5? | ✅ Evet (`ENTRY_SLOT_RATIO = 0.20`) |
| DERR'e yazılıyor mu? | ✅ Evet, `signal_source=MANUEL` |

Kaldıraç whitelist + marjin = slot/5:

```32:34:scripts/manual_open.py
LEVERAGE_DEFAULT = 4
ENTRY_SLOT_RATIO = 0.20
ALLOWED_LEVERAGES = frozenset({1, 2, 3, 4, 5, 10})
```

```119:123:scripts/manual_open.py
    account = AccountManager(client)
    slot = account.calculate_slot_size()
    margin = slot * ENTRY_SLOT_RATIO
    journal = TradingJournal(os.path.join(ROOT, "mina_trading_journal.db"))
    mina = MinaPositionManager(client, slot, journal=journal, data_root=ROOT)
```

DERR kaydı `MANUEL` kaynağıyla:

```201:201:scripts/manual_open.py
    mina.log_position_open(symbol, side, args.leverage, mark, qty, margin, signal_source=MANUEL)
```

Dashboard her zaman `source='haluk'` gönderiyor:

```429:436:dashboard/dashboard_ws.py
        cmd = [
            os.path.join(ROOT, 'venv/bin/python'),
            os.path.join(ROOT, 'scripts/manual_open.py'),
            '--symbol', sym,
            '--side', sd,
            '--leverage', str(lev),
            '--source', 'haluk',
        ]
```

**⚠️ Notlar:**
1. **Slot sayımı kaba:** `_count_motor_slots` her 4x pozisyonu Haluk sayıyor; manuel açılan bir SHORT da Haluk slotundan düşülüyor. Kaynak ayrımı zayıf (satır 60-62, `merter += 0` placeholder).
2. **Manuel non-4x'te resting stop yok:** Manuel 3x/5x/10x açılışında borsaya **duran STOP emri konmuyor**; stop yalnızca motor döngüsü ayakta olduğu sürece spot-fiyat izlemesiyle market kapatılıyor (`evaluate_position → stop_loss`). Motor çökerse stop korumasız.

---

## BÖLÜM 3 — KALDIRAÇ BAZLI KURALLAR

Kaynak: `mina_position_manager.py` satır 65-93.

| Kaldıraç | TP1 | TP1 kapat | TP2 | TP2 kapat | Trailing | Stop-Loss | Savunma | Breakeven |
|---|---|---|---|---|---|---|---|---|
| **1x** | +%3 | %50 | +%5 | %50 | %2 callback | **%3** | Yok | TP1 sonrası → avg |
| **2x** | +%3 | %50 | +%5 | %50 | %2 | **%3** | Yok | TP1 sonrası → avg |
| **3x** | +%3 | %50 | +%5 | %50 | %2 | **%2** | Yok | TP1 sonrası → avg |
| **4x** | +%3 | %50 | +%5 | %50 | %2 | **YOK** | ✅ D1/D2/D3 | D2/D3 = avg×**1.0035** |
| **5x** | +%3 | %50 | +%5 | %50 | %2 | **%2** | Yok | — |
| **10x** | +%2 | %50 | +%4 | **%100** | **YOK** | **%1** | Yok | — |

(Ayrıca kodda 6x=%2, 7x=%1.5, 8x/9x=%1 var ama manuel açılışa kapalı.)

```78:99:mina_position_manager.py
        self.tp_rules = {
            'standard': {
                'tp1_ratio': 0.50,
                'tp2_ratio': 0.50,
                'tp1_multiplier': 1.03,
                'tp2_multiplier': 1.05,
                'trailing_callback_pct': 2.0,
            },
            'fast': {
                'tp1_ratio': 0.50,
                'tp2_ratio': 1.00,
                'tp1_multiplier': 1.02,
                'tp2_multiplier': 1.04,
                'trailing_callback_pct': None,
            }
        }

        self.defense_rules = {
            1: {'trigger_multiplier': 0.95, 'slot_ratio': 0.20},
            2: {'trigger_multiplier': 0.88, 'slot_ratio': 0.20},
            3: {'trigger_multiplier': 0.75, 'slot_ratio': 0.40},
        }
```

TP1 sonrası breakeven stop kuruluyor (anayasaya uyumlu):

```1034:1044:mina_position_manager.py
                stop_price = self._round_price(state.get('weighted_avg_price', position.get('entry_price')))
                try:
                    self.client.futures_create_order(
                        symbol=symbol,
                        side=order_side,
                        type=ORDER_TYPE_STOP_MARKET,
                        stopPrice=stop_price,
                        quantity=remaining_qty,
                        positionSide=side
                    )
                    print(f"   🛡️  Breakeven stop order kuruldu: {stop_price}")
```

**✅ Anayasaya tam uyumlu.** Tek not: standart breakeven stop'u `weighted_avg`'a (komisyonsuz) çekiyor; D2/D3 ise `×1.0035` ekliyor. Tutarlı.

---

## BÖLÜM 4 — SAVUNMA SİSTEMİ (4x)

| Bileşen | Koşul | Ekleme | Durum |
|---|---|---|---|
| **D1** | `spot ≤ entry×0.95` | `slot/5` market | ✅ Stop yok, TP devam, idempotent |
| **D2** | `spot ≤ entry×0.88` | `slot/5` market | ✅ TP dondurulur, BE=avg×1.0035 TAKE_PROFIT_MARKET |
| **D3** | `spot ≤ entry×0.75` **+ SFP onayı** | `slot×0.40` (=2/5) | ⚠️ SFP basitleştirilmiş |
| **Hard Stop** | `spot ≤ entry×0.75` (SFP yoksa) | Tam kapama market | ✅ |
| D2 TP dondurma | `tp_disabled=True` | — | ✅ Çalışıyor |
| D3 başabaş | avg×1.0035 + D2 iptal | — | ✅ Çalışıyor |
| **4 saatlik zaman stopu (D2)** | — | — | ❌ **YOK (motorda zaman stopu yok)** |

D1 idempotency (restart sonrası tekrar market emri atmaz) — anayasaya uyumlu:

```658:662:mina_position_manager.py
        journal_defense = self._defense_level_from_journal(symbol, side)
        if journal_defense >= 1:
            state['defense_stage'] = max(stage, file_defense, journal_defense, 1)
            self._save_state()
            return True
```

D2: ekleme + TP dondurma + başabaş kaçış emri:

```733:745:mina_position_manager.py
        weighted_avg = ((amount * float(position.get('entry_price'))) + (add_qty * current_price)) / total_qty
        state['weighted_avg_price'] = weighted_avg
        state['tp_disabled'] = True

        breakeven_price = self._round_price(weighted_avg * 1.0035)
        escape_side = SIDE_SELL if side == 'LONG' else SIDE_BUY

        try:
            order = self.client.futures_create_order(
                symbol=symbol,
                side=escape_side,
                type=ORDER_TYPE_TAKE_PROFIT_MARKET,
                stopPrice=breakeven_price,
```

**D3 SFP algoritması — KODLANMIŞ ama BASİTLEŞTİRİLMİŞ (placeholder değil):**

```932:957:mina_position_manager.py
        lows_4h = [float(k[3]) for k in klines_4h]
        opens_4h = [float(k[1]) for k in klines_4h]
        closes_4h = [float(k[4]) for k in klines_4h]
        support_low = min(lows_4h[-8:])
        support_high = support_low * 1.003
        ...
        if last_5m_close <= last_5m_open:
            return False
        if not (support_low <= last_5m_close <= support_high):
            return False
```

⚠️ **Eksik:** Anayasanın istediği **"min 3 temas"** kuralı yok. Destek = son 8 adet 4H mumun en düşüğü olarak alınıyor; temas sayımı yapılmıyor. 5m bull bar onayı var. Yani SFP **gerçek kod** ama anayasanın tam algoritması değil.

**❌ 4 saatlik zaman stopu D2 için UYGULANMIYOR:** Motorda (Haluk 4x) hiçbir zaman stopu yok. Zaman stopu **yalnızca Merter** tarafında (`MAX_HOLD_H`). Motorda yalnızca dolmamış limit emirleri için 8 saatlik TTL var (`cancel_stale_pending_limits`), açık D2 pozisyonu için zaman stopu yok.

---

## BÖLÜM 5 — MERTER 1x DCA SİSTEMİ

| Soru | Cevap |
|---|---|
| EI-1 (süzgeçli) + EI-2 (süzgeçsiz) + other (RSI) | ✅ Çalışıyor |
| RSI<20 + teyit | ✅ Var |
| RVOL≥2.0 | ✅ Var |
| %2 × 9 limit DCA | ✅ Var |
| TP1/TP2/trailing | ✅ +%3/%50, +%5, trailing %2 |
| 4 saatlik zaman stopu | ✅ Çalışıyor (ama anayasa 48s diyor) |
| Breakeven çarpanı 1.0020 | ✅ Var |
| Motor karışmıyor | ✅ `is_merter_dca_position` guard + coin lock |

DCA merdiveni `%2 × n`:

```513:514:signal_bot/merter_dca_manager.py
        for n in range(start_step, start_step + count):
            price = self._round_price(symbol, anchor * (1.0 - DCA_STEP * n))
```

RSI teyit (+3 puan veya 20 kırılımı):

```174:182:signal_bot/merter_dca_manager.py
        if r >= RSI_OVERSOLD:
            ...
            if r_next >= RSI_OVERSOLD or r_next >= r + 3.0:
```

TP1/TP2/trailing/breakeven monitör döngüsü:

```918:931:signal_bot/merter_dca_manager.py
            if not pos.get("tp1_done") and mark >= tp1:
                close_qty = self._round_qty(symbol, qty * 0.5)
                ...
            if pos.get("tp1_done") and not pos.get("trailing_active") and mark >= tp2:
                pos["trailing_active"] = True
```

Motor Merter'e karışmıyor:

```427:429:mina_position_manager.py
            from ghost_positions import is_merter_dca_position
            if is_merter_dca_position(symbol, side, leverage):
                return {'action': 'hold', 'reason': 'Merter DCA yönetiminde'}
```

**✅ EMA20 filtresi aslında KODLANMIŞ** (anayasa backlog'u güncel değil) — `merter_ei_1` süzgeçli yuvada:

```576:579:signal_bot/signal_parser.py
    if direction == "LONG" and mark <= ema20:
        return False, "Adım1: fiyat EMA20 altında", 0.0, {"mark": mark, "ema20": ema20}
    if direction == "SHORT" and mark >= ema20:
        return False, "Adım1: fiyat EMA20 üstünde (SHORT)", 0.0, {"mark": mark, "ema20": ema20}
```

⚠️ Ama EMA20 yalnızca **`merter_ei_1`** (süzgeçli) için; `merter_ei_2` tasarımca süzgeçsiz, RSI yuvasında EMA20 yok.

---

## BÖLÜM 6 — HALUK HOCA SİNYALLERİ

| Soru | Cevap |
|---|---|
| 4x stratejisi | ✅ `LEVERAGE = 4` (slot bridge) |
| Grafik + giriş → approved, yoksa rejected | ✅ Doğru |
| Stop aranıyor mu? | ✅ HAYIR — aranmıyor (doğru, savunma var) |
| Limit/market mantığı | ✅ Doğru |
| Görsel parser BTC/TOTAL | ✅ Çalışıyor (2026-06-06 doğrulandı) |
| Bugüne kadar approved | ⚠️ Kuyruk şu an 0; DERR'de HT etiketli oto-işlem yok |

Onay mantığı (giriş bölgesi + grafik şart, stop şart değil):

```408:417:signal_bot/haluk_pdf_parser.py
    has_entry_zone = bool(re.search(r"giriş\s*bolgesi|giris\s*bolgesi|entry\s*zone", sec.raw_text, re.I))
    has_chart = bool(CHART_KEYWORDS_RE.search(sec.raw_text)) or has_entry_zone

    if not (has_chart and has_entry):
        ...
            "reason": "no_chart_or_entry",
```

Limit/market kararı (anayasaya uyumlu):

```38:43:mina_entry_orders.py
    side = side.upper()
    if side == "LONG" and entry_price < mark:
        return ORDER_TYPE_LIMIT, entry_price
    if side == "SHORT" and entry_price > mark:
        return ORDER_TYPE_LIMIT, entry_price
    return ORDER_TYPE_MARKET, None
```

**⚠️ Approved sinyal sayısı — DERR gerçeği (sunucudan 2026-06-06):**
- `raw_signal_queue.json`: **0 entry** (şu an boş)
- DERR `signal_source` dağılımı: `None=20` (eski/kolon öncesi), `MANUEL=6`, `MZ=5`, `merter_*=6`
- **Hiçbir işlemde `HT` etiketi yok.** Yani PDF→otomatik açılış pipeline'ı DERR'e **HT etiketli** işlem yazmamış. Haluk sinyalleri ya hiç oto-açılmadı ya da `None` kovasında (kolon eklenmeden önce).

Bu, Haluk oto-açılış zincirinin **uçtan uca canlı test edilmediğini** gösteriyor — kritik bulgu.

---

## BÖLÜM 7 — GÜVENLİK

| Konu | Durum |
|---|---|
| API key saklama | ✅ `.env` + `os.getenv`, `.env` **gitignore'da ve takipsiz** |
| `.env` GitHub'a gidiyor mu? | ✅ **HAYIR** (`git check-ignore .env` → ignored, takipte değil) |
| **SSH şifresi repoda** | ❌ **SIZINTI** — deploy scriptlerinde gömülü |
| Race condition | ⚠️ JSON state kilitsiz, TOCTOU riski |
| Duplicate order koruması | ⚠️ Kısmi (fingerprint + margin dosyası, ama `newClientOrderId` yok) |
| Restart sonrası açık emirler | ⚠️ Resting TP/stop yeniden konmaz; motor down ise korumasız |
| Rate limit koruması | ❌ **YETERSİZ** (274 hata sebebi) |

**❌ KRİTİK — SSH şifresi düz metin, repoda:**

```12:14:scripts/deploy_full.py
HOST, USER = "178.105.150.40", "root"
PASS = os.environ.get("MINA_SSH_PASS", "REDACTED")
REMOTE = "/root/MINA_v2"
```

`root` şifresi onlarca scriptte fallback default olarak gömülü. Repo public veya sızarsa sunucu tamamen ele geçirilir. **Acil:** şifreyi sıfırla, SSH anahtarına geç, tüm scriptlerden default'u kaldır.

**❌ Rate limit (-1003, 274 hata):**
- Motor 30s döngü, tek `futures_position_information()` çağrısı (iyi).
- **Ama Merter `_round_price` her limit emirde `futures_exchange_info()` çağırıyor** — 1 açılış = 9 ağır çağrı:

```459:467:signal_bot/merter_dca_manager.py
    def _round_price(self, symbol: str, price: float) -> float:
        info = self._client_get().futures_exchange_info()
```

- **Ban anında backoff yok:** Motor `-1003 banned` alınca sadece logluyor ve 30s sonra tekrar deniyor → ban penceresi boyunca her 30s'de yeni hata. `main.py` döngüsünde `except` sadece `time.sleep(interval)`:

```119:121:main.py
        except Exception as exc:
            logger.exception("Döngü hatası: %s", exc)
            time.sleep(interval)
```

- 5 ayrı servis (engine 30s, merter 30s, dashboard-ws 15s, queue-watcher, listener) her biri bağımsız polling → kümülatif yük. **Ban süresini okuyup bekleyen ortak backoff yok.**

**⚠️ Race / kilit:** `mt.load_json`/`save_json` atomik değil ve dosya kilidi yok. Engine + merter + dashboard subprocess aynı tracking dosyalarına yazıyor. Coin lock cross-bot çakışmayı azaltıyor ama JSON yazımları yarış halinde bozulabilir.

---

## BÖLÜM 8 — ÇALIŞMAYAN / EKSİK MODÜLLER

| Modül | Durum |
|---|---|
| D3 SFP algoritması | ⚠️ **Kodlanmış ama basitleştirilmiş** (3 temas yok) |
| `/fapi/v1/order/algo/trailing` | ❌ **Hiç kullanılmıyor / test edilmemiş** |
| Order retry mekanizması | ❌ **YOK** (çekirdekte hiç retry/backoff yok) |
| RSI>70 LONG reddet | ❌ **Yok** (momentum guard kodlanmamış) |
| EMA20 filtresi | ✅ **Kodlanmış** (`merter_ei_1` süzgeçli) |
| Global risk limiti | ❌ **Yok** (slot cap var, portföy-seviye yok) |

- **Algo trailing endpoint:** Tüm repoda `order/algo/trailing` yok. Motor testnet'te manuel trailing (`max_prices`) + `TRAILING_STOP_MARKET` kullanıyor. Canlı algo endpoint **hiç test edilmedi** (anayasa zaten zorunlu kılıyor).
- **Order retry:** `_with_retry`/`max_retries`/backoff hiçbir çekirdek dosyada yok. Emir bir kez denenir, hata olursa pozisyon/savunma atlanır.
- **RSI>70 LONG reddet:** EI filtresinde sadece EMA20 ve SR/pattern var; RSI>70 momentum reddi yok. Anayasa backlog'unda açık.
- **Global risk limiti:** Yalnızca `MAX_POSITIONS=8` ve slot başına marjin tavanı (`_slot_limit_check`) var. Günlük zarar / toplam drawdown kill-switch **yok**. (DERR'de toplam kapanmış PnL **-307.81 USDT**, 33 işlemde 16 kazanç — testnet.)

---

## BÖLÜM 9 — CANLI HESAP RİSKLERİ

**1. Testnet'te çalışıp canlıda farklı davranacaklar:**
- **D2/D3/breakeven kaçış emirleri:** Testnet'te `TAKE_PROFIT_MARKET`/conditional emirler **-4120 ile reddediliyor** (anayasa not). Yani D2/D3 başabaş emirleri **şu an testnet'te muhtemelen sessizce hata veriyor** → savunma kaçışı testnet'te doğrulanamamış durumda. Canlıda çalışır ama hiç test edilmedi.
- Manuel trailing (`max_prices`) → canlıda gerçek `TRAILING_STOP_MARKET`/algo'ya geçiş gerekecek.

**2. `BINANCE_TESTNET=false` yapınca ek değişiklikler:**
- Gerçek API key/secret (`.env`)
- **Hedge mode doğrulaması:** Kod her yerde `positionSide="LONG"/"SHORT"` kullanıyor → hesap **Hedge Mode** olmalı, yoksa emirler reddedilir.
- Kaldıraç braketleri (canlıda coin başına max kaldıraç farklı olabilir)
- Algo trailing endpoint testi (anayasa zorunlu)
- Bakiye/min notional kontrolleri

**3. Trailing canlıda:** `TRAILING_STOP_MARKET` callbackRate=2.0, activationPrice=TP2 ile çalışır; testnet'teki -4120 sorunu kalkar. Ama davranış hiç gözlemlenmedi.

**4. İlk hafta kaç slot:** **1–2 slot** öneriyorum. Önce tek bir 4x Haluk pozisyonuyla D1→D2→D3→breakeven döngüsünün **tamamını** canlı conditional emirlerle doğrula.

---

## BÖLÜM 10 — GENEL DEĞERLENDİRME

### Olgunluk: **6 / 10**
Çekirdek strateji mantığı (TP/D1/D2/D3/SFP/Merter DCA) doğru ve anayasaya büyük oranda uyumlu kodlanmış. Ancak operasyonel sağlamlık (rate limit, retry, kilitler), güvenlik (SSH şifresi) ve canlı conditional emir doğrulaması eksik.

### Canlıya geçmek için kaç işlem daha?
Anayasa **50 temiz işlem** istiyor. Şu an DERR'de **33 kapanmış** var ama çoğu legacy (`None`=20) + manuel + Merter. **Temiz 4x Haluk motor işlemi neredeyse yok.** Tahminim: **savunma döngüsü tetiklenen en az 30–40 temiz motor işlemi** daha (özellikle D2/D3'ün canlı conditional emirlerle çalıştığı en az 5–10 vaka).

### EN KRİTİK 3 RİSK
1. **SSH root şifresi repoda düz metin** (`scripts/deploy_full.py:13` ve onlarca script) — sunucu ele geçirme riski. **Acil sıfırla + SSH key.**
2. **D2/D3/breakeven conditional emirleri canlıda hiç doğrulanmadı** — testnet'te -4120 ile reddedildikleri için savunma kaçışının gerçekten çalışıp çalışmadığı **bilinmiyor**. Para riski en yüksek nokta.
3. **Rate-limit storm + retry yok** — ban anında backoff olmadığı için 274 hata; ban penceresinde savunma/TP tetikleyicileri **kaçabilir** (emir atılamaz, retry yok).

### EN KRİTİK 3 EKSİK
1. **Order retry/backoff mekanizması** (çekirdekte hiç yok) — `-1003`/geçici hatalarda savunma emirleri sessizce düşüyor.
2. **Algo trailing endpoint testi** + canlı conditional emir doğrulaması.
3. **Global risk limiti (günlük zarar kill-switch)** + D3 SFP'nin **3 temas** kuralı + RSI>70 LONG reddi.

---

## ÖZET ÖNCELİK SIRASI

1. SSH şifresi → sıfırla, SSH key'e geç, scriptlerden default kaldır
2. Rate-limit backoff + retry
3. Canlı D2/D3 conditional emir testi
4. Global kill-switch
5. Merter 4s/48s çelişkisini netleştir
6. `backend/config.py` ölü tabloyu temizle
7. SFP 3-temas + RSI>70

---

*Rapor: Opus 4.8 tam sistem denetimi — 2026-06-06*
